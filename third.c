#include <stdio.h>
int main()
{
    int x, y, z;
    printf("welcome to BTECH G2G\n");
    printf("enter the value of x and y -%d %d", x, y);
    scanf("%d %d", &z, &y);
    z = y + x;
    printf("sum is %d", z);
    return 0;
}
